[build]
  base = "."
  command = "pnpm install --no-frozen-lockfile && pnpm run typecheck:libs && cd artifacts/fraud-dashboard && pnpm run build"
  publish = "artifacts/fraud-dashboard/dist/public"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_URL = "https://fraud-9lts.onrender.com/api"
  PNPM_VERSION = "10"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
